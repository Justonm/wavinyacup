; php.ini - Basic configuration for development

; Display all errors for debugging
display_errors = On
error_reporting = E_ALL

; --- Session Configuration ---
; Enable session module
session.save_handler = files
; Set a writable path for session files
session.save_path = "/home/justus/wavinyacup/sessions"

; --- File Upload Configuration ---
; Increase limits to allow for larger profile photos
upload_max_filesize = 25M
post_max_size = 25M
